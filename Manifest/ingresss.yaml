---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pievent-ingress
  namespace: hamzadevops
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod  # Uses Let's Encrypt for SSL
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/use-regex: "true"  # Keep this from your old working config
spec:
  ingressClassName: nginx
  rules:
  - host: myapp.example.com  # Change to your actual domain
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: pievent-service
            port:
              number: 8083
      - path: /
        pathType: Prefix
        backend:
          service:
            name: angular-service
            port:
              number: 80
  tls:
  - hosts:
    - myapp.example.com  # Make sure this domain points to your cluster
    secretName: myapp-tls  # Cert-Manager will create this secret automatically
